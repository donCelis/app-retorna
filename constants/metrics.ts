import { generateSizes } from "@/utils/generateSizes";

export const sizes = generateSizes(14, 4);

export const HEIGHT_IMAGE_CARD = 250;

export const ANIMATION_IMAGE_DEFAULT = { duration: 100 };

export const START_TOTAL = 5;
